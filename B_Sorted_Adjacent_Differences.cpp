#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tt;cin>>tt;
    while(tt--){
       int n;cin>>n;
       vector<int> a(n);for(auto &i:a) cin>>i;
       sort(a.begin(),a.end());
       vector<int> ans;
       if(n%2==0){
        for(int i=n-1;)
       }
       cout<<"\n";
    }
    return 0;
}