#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define rall(x) x.rbegin(),x.rend()
#define all(x) x.begin(),x.end()
const int MAX=3005;
int main()
{
fastio;
    int tt;cin>>tt;
    while(tt--){
       int n;cin>>n;
       vector<string> a(n);for(auto &i:a) cin>>i;
       for(int x=0;x<1.414*n;x++){
        for(int y=0;y<1.414*n;y++){
            
        }
       }
    }
    return 0;
}