#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define fastio ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define rall(x) x.rbegin(),x.rend()
#define all(x) x.begin(),x.end()
vector<char> cmds;
ll calc(int i,int j){
    if(cmds[j]=='f'){
        calc()
    }
}

int main()
{
fastio;
    const ll MOD=1e9+7;
    int n;cin>>n;
    cmds.assign(n,' ');for(auto &i:cmds) cin>>i;
    
    return 0;
}